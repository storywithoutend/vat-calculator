interface MetaData {
  source: 'amazon' | 'shopify'
  version: 1
  startDate: Date
  endDate: Date
  items: number
}

export const getMetaData = (data: unknown[]): MetaData => {
  const 
  return [

  ]
}